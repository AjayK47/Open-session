{
  "name": "open-session",
  "version": "0.0.0",
  "private": true,
  "description": "Open-source conference program operating system (CFP → Review → Accept → Onboard → Schedule)",
  "scripts": {
    "dev": "pnpm --parallel --filter @opensession/web dev",
    "dev:api": "cd apps/api && uv run uvicorn app.main:app --reload --port 8000",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "packageManager": "pnpm@10.30.3",
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
